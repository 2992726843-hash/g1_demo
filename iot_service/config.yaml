# Home Assistant 接入配置
ha_url: "http://192.168.122.5:8123" # ← 替换成真实的Home Assistant URL，具体地址可在HA中查看
#tyx
#token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJiOGM0MjM2NzAwNDk0YjQxYWY4N2RkMTVkMDM4MjMxNiIsImlhdCI6MTc3NzcyNjc5MywiZXhwIjoyMDkzMDg2NzkzfQ.AAaJO9ReMP2RBiIm7kk6ufp2epAxSgs2gG0LYpGLo_4"
#lyw
token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJkODI3YTU4ZTkxOTg0YTI5YWZiZmE1YjNjNzg1ODZhNyIsImlhdCI6MTc3Nzc4MTIwNiwiZXhwIjoyMDkzMTQxMjA2fQ.q1PTsdgexSGM3z-spTU5CASLKRWmll82_-mCYOURUhQ"
# 本服务监听地址
server_host: "0.0.0.0"
server_port: 5001

# HTTP 超时（秒）
timeout: 5

# 是否启用 Mock 模式（无需真实 HA 即可调试）
# true  -> 使用内置模拟后端，并开放 /mock/* 管理接口
# false -> 使用真实 Home Assistant
mock: false   # ← 改成 false 才会真正调用 HA

# 日志级别: DEBUG / INFO / WARNING / ERROR
log_level: "INFO"

# --------------------------------------------------------------------------- #
# 自定义场景（可选）
# 每个场景会自动注册为 POST /iot/scene/{name} 接口。
# steps 字段说明：
#   device  - devices.yaml 中的逻辑名或直接的 HA entity_id
#   action  - on / off / toggle
#   其余字段均作为 HA 属性透传（brightness、rgb_color、color_temp、transition 等）
# --------------------------------------------------------------------------- #
scenes:
  morning_mode:
    description: "早安模式：全亮暖白光"
    steps:
      - device: living_room_light
        action: on
        brightness: 255
        color_temp: 3000
        transition: 2
      - device: bedroom_light
        action: on
        brightness: 200

  good_night:
    description: "晚安模式：关主灯，床头常亮小夜灯"
    steps:
      - device: living_room_light
        action: off
      - device: bedroom_light
        action: off
      - device: night_light_socket
        action: on
